**[🇷🇺 Русский](README_RU.md) | [🇬🇧 English](README.md)**


## ESModManager-Go

CLI утилита для управления модами игры **Бесконечное Лето**, написанная на Go. Позволяет включать и выключать моды, автоматически определять информацию о них, запускать игру с только нужными модами.

---

## Возможности

* **Извлечение информации** из `.rpy` файлов Ren'Py
* **Получение названий модов через Steam API**
* **Включение/отключение модов** по папке или codename

---

## Установка

```bash
go build
```
(Скоро будет meson.build)

---

## Использование

### Показать список модов
```bash
./esmodmanager list
```

### Включить мод
```bash
./esmodmanager enable <папка|codename>
```

### Выключить мод
```bash
./esmodmanager disable <папка|codename>
```

### Включить/выключить все моды
```bash
./esmodmanager enable ALL
./esmodmanager disable ALL
```

### Запустить игру
```bash
./esmodmanager launch
```

---

## Конфигурация

При первом запуске программа автоматически создаёт два файла:

`config.yaml` — конфигурация программы (редактируемый)

Содержит пути и параметры запуска игры.

```yaml
game_exe: /usr/bin/steam
args:
  - -applaunch
  - "331470"
workshop_root: /home/user/.steam/steam/steamapps/workshop/content/331470
disabled_dir: /home/user/.elmod_disabled
```

`mods_db.yaml` — база данных модов

Содержит список найденных модов и их состояние.

```yaml
mods:
  - name: Example Mod
    codename: example
    folder: "1234567890"
    enabled: true
    discovered_at: 2024-12-17T13:42:11Z
```

---

## Как это работает

1. Программа сканирует папку Steam Workshop.
2. Для каждого мода пытается определить:
   * codename в `.rpy` файлах
   * название
3. При отсутствии данных делает запрос в Steam API.
4. Выключенные моды временно переносятся в отдельную директорию.
5. После выхода из игры всё восстанавливается.

---

## Ограничения

* Программа адаптирована только под Linux (возможно, временно)

---

## Лицензия

MIT
